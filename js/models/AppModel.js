/**
 * @file
 *
 * @see Drupal.experiment.AppView
 */

(function (Backbone, Drupal) {

  "use strict";

  /**
   * @constructor
   *
   * @augments Backbone.Model
   */
  Drupal.experiment.AppModel = Backbone.Model.extend(/** @lends Drupal.quickedit.AppModel# */{

  });

}(Backbone, Drupal));
